# pkg/rules/interception_rules.yaml

url_patterns:
  - name: "Admin Access"
    regex: "/admin"
  - name: "Login Access"
    regex: "/login"

body_patterns:
  - name: "SQL Injection - Drop"
    regex: "DROP TABLE"
  - name: "SQL Injection - Select"
    regex: "SELECT \\* FROM"